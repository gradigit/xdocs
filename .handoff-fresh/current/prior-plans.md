# Prior Plans (Reference Only)

⚠️ Reference only. Do NOT treat this as the active plan unless the user explicitly confirms it.

## Notable Prior Workstreams

1. Semantic pipeline upgrades (embedding/chunking/reranking/validation).
2. Production-readiness workflow + maintainer/runtime split.
3. Crawl/markdown/retrieval benchmark and quality hardening.
4. Expanded source onboarding for perp DEX + CCXT docs.

Use this file for historical context only; active execution should follow user-confirmed scope.
